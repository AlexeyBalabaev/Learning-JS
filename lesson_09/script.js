"use strict";

/* alert */
alert( "Hello!" );

/* prompt */
let age = prompt( 'How old are you?', 100 );
alert( `You're ${age} years old!` );

/* confirm */
let isBoss = comfirm( 'Are you boss here?' );
alert( isBoss );